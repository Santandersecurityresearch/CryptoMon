pymongo==4.8.0
fastapi==0.111.1
jc==1.25.3
motor==3.5.1
pydantic==2.13.5
pydantic-settings==2.15.0
tinydb==4.8.0
uvicorn==0.30.0
psutil==6.0.0
scapy==2.5.0

# Certificate parsing for the offline pipeline (pcapscan.certificates).
# Optional at runtime -- pcapscan.sessions carries the raw DER when it is
# absent -- but pinned here so CI and a default install have it. 39.0.1 was
# what the environment happened to hold; 44.x is current and has the
# not_valid_*_utc accessors and public_key_algorithm_oid that the CBOM
# export needs.
cryptography>=44.0.2,<45

# The capture upload UI (fapi/app/uploads.py). Both arrive transitively with
# fastapi today, which is exactly why they are pinned here: a transitive
# dependency is one somebody else's release notes can remove.
jinja2==3.1.6
python-multipart==0.0.20
